# 전체 스택 = 인프라 + 데이터/메시징 + 모든 서비스
#
# 데이터 계층은 compose.data.yaml에 정의돼 있고 여기서 include한다.
# 중복 정의하면 두 파일이 조용히 어긋난다.
include:
  - compose.data.yaml

services:

  consul-1:
    image: hashicorp/consul:1.18
    container_name: lxp-consul-1
    command: agent -server -ui -node=consul-1 -bootstrap-expect=3 -client=0.0.0.0 -retry-join=consul-2 -retry-join=consul-3
    ports:
      - "8500:8500"
    networks: [lxp-net]
    healthcheck:
      test: ["CMD", "consul", "members"]
      interval: 10s
      timeout: 5s
      retries: 10

  consul-2:
    image: hashicorp/consul:1.18
    container_name: lxp-consul-2
    command: agent -server -node=consul-2 -bootstrap-expect=3 -client=0.0.0.0 -retry-join=consul-1 -retry-join=consul-3
    networks: [lxp-net]

  consul-3:
    image: hashicorp/consul:1.18
    container_name: lxp-consul-3
    command: agent -server -node=consul-3 -bootstrap-expect=3 -client=0.0.0.0 -retry-join=consul-1 -retry-join=consul-2
    networks: [lxp-net]


  prometheus:
    image: prom/prometheus:v3.1.0
    container_name: lxp-prometheus
    command:
      - --config.file=/etc/prometheus/prometheus.yml
    volumes:
      - ./infrastructure/prometheus/prometheus-docker.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks: [lxp-net]

  grafana:
    image: grafana/grafana:11.4.0
    container_name: lxp-grafana
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
    volumes:
      - grafana-data:/var/lib/grafana
      - ./infrastructure/grafana/provisioning:/etc/grafana/provisioning:ro
    ports:
      - "3000:3000"
    depends_on:
      - prometheus
      - loki
    networks: [lxp-net]

  loki:
    image: grafana/loki:3.3.2
    container_name: lxp-loki
    command: -config.file=/etc/loki/local-config.yaml
    volumes:
      - ./infrastructure/loki/loki-config.yml:/etc/loki/local-config.yaml:ro
      - loki-data:/loki
    ports:
      - "3100:3100"
    networks: [lxp-net]

  alloy:
    image: grafana/alloy:latest
    container_name: lxp-alloy
    command:
      - run
      - /etc/alloy/config.alloy
      - --server.http.listen-addr=0.0.0.0:12345
      - --storage.path=/var/lib/alloy/data
    volumes:
      - ./infrastructure/alloy/config.alloy:/etc/alloy/config.alloy:ro
      - ./infrastructure/logs:/var/log/lxp:ro
      - alloy-data:/var/lib/alloy/data
    ports:
      - "12345:12345"
    depends_on:
      - loki
    networks: [lxp-net]

  zipkin:
    image: openzipkin/zipkin:3.4
    container_name: lxp-zipkin
    ports:
      - "9411:9411"
    networks: [lxp-net]


  gateway:
    build:
      context: .
      dockerfile: ./gateway/Dockerfile
    container_name: lxp-gateway
    environment:
      CONSUL_HOST: consul-1
      CONSUL_PORT: 8500
      LOG_DIR: /logs
      CONFIG_SERVER_URL: http://config-server:8888
      ZIPKIN_ENDPOINT: http://zipkin:9411/api/v2/spans
    ports:
      - "8080:8080"
    volumes:
      - ./infrastructure/logs:/logs
    depends_on:
      consul-1:
        condition: service_healthy
      config-server:
        condition: service_healthy
    networks: [lxp-net]
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8080/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  config-server:
    build:
      context: .
      dockerfile: ./config-server/Dockerfile
    container_name: lxp-config-server
    environment:
      CONSUL_HOST: consul-1
      CONSUL_PORT: 8500
      LOG_DIR: /logs
      CONFIG_SEARCH_LOCATIONS: file:/config-repo
      ZIPKIN_ENDPOINT: http://zipkin:9411/api/v2/spans
    ports:
      - "8888:8888"
    volumes:
      - ./infrastructure/logs:/logs
      - ./config-repo:/config-repo:ro
    depends_on:
      consul-1:
        condition: service_healthy
    networks: [lxp-net]
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8888/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  member-service:
    build:
      context: .
      dockerfile: ./member-service/Dockerfile
    container_name: lxp-member-service
    environment:
      CONSUL_HOST: consul-1
      CONSUL_PORT: 8500
      LOG_DIR: /logs
      CONFIG_SERVER_URL: http://config-server:8888
      ZIPKIN_ENDPOINT: http://zipkin:9411/api/v2/spans
      # 컨테이너끼리는 항상 내부 포트 3306으로 통신한다.
      # 호스트 포트(MYSQL_PORT)와는 무관하다.
      MYSQL_HOST: mysql
      MYSQL_DB_PORT: 3306
      MYSQL_USER: member
      MYSQL_PASSWORD: member
      GRPC_SERVER_PORT: 9092
    ports:
      - "8082:8082"
      - "9092:9092"   # gRPC (subscription-service가 호출)
    volumes:
      - ./infrastructure/logs:/logs
    depends_on:
      consul-1:
        condition: service_healthy
      config-server:
        condition: service_healthy
      mysql:
        condition: service_healthy
    networks: [lxp-net]
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8082/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  course-service:
    build:
      context: .
      dockerfile: ./course-service/Dockerfile
    container_name: lxp-course-service
    environment:
      CONSUL_HOST: consul-1
      CONSUL_PORT: 8500
      LOG_DIR: /logs
      CONFIG_SERVER_URL: http://config-server:8888
      ZIPKIN_ENDPOINT: http://zipkin:9411/api/v2/spans
      MONGO_HOST: mongo
      MONGO_DB_PORT: 27017
      # 서버가 객체 존재를 확인할 때 쓰는 내부 주소
      MINIO_INTERNAL_ENDPOINT: http://minio:9000
      # presigned URL 서명 주소 — 브라우저가 접속할 주소여야 한다.
      # 내부 주소로 서명하면 URL이 도커 밖에서 열리지 않는다.
      MINIO_PUBLIC_ENDPOINT: http://localhost:${MINIO_PORT:-9000}
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin}
    ports:
      - "8083:8083"
    volumes:
      - ./infrastructure/logs:/logs
    depends_on:
      consul-1:
        condition: service_healthy
      config-server:
        condition: service_healthy
      mongo:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks: [lxp-net]
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8083/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  subscription-service:
    build:
      context: .
      dockerfile: ./subscription-service/Dockerfile
    container_name: lxp-subscription-service
    environment:
      CONSUL_HOST: consul-1
      CONSUL_PORT: 8500
      LOG_DIR: /logs
      CONFIG_SERVER_URL: http://config-server:8888
      ZIPKIN_ENDPOINT: http://zipkin:9411/api/v2/spans
      MYSQL_HOST: mysql
      MYSQL_DB_PORT: 3306
      MYSQL_USER: subscription
      MYSQL_PASSWORD: subscription
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_AMQP_PORT: 5672
    ports:
      - "8084:8084"
    volumes:
      - ./infrastructure/logs:/logs
    depends_on:
      consul-1:
        condition: service_healthy
      config-server:
        condition: service_healthy
      mysql:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    networks: [lxp-net]
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8084/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  payment-service:
    build:
      context: .
      dockerfile: ./payment-service/Dockerfile
    container_name: lxp-payment-service
    environment:
      CONSUL_HOST: consul-1
      CONSUL_PORT: 8500
      LOG_DIR: /logs
      CONFIG_SERVER_URL: http://config-server:8888
      ZIPKIN_ENDPOINT: http://zipkin:9411/api/v2/spans
      MYSQL_HOST: mysql
      MYSQL_DB_PORT: 3306
      MYSQL_USER: payment
      MYSQL_PASSWORD: payment
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_AMQP_PORT: 5672
      # 정기 결제 (D-25). 데모로 짧은 주기를 보려면 .env에서 조정한다.
      BILLING_PERIOD_DAYS: ${BILLING_PERIOD_DAYS:-30}
      BILLING_SCHEDULER_ENABLED: ${BILLING_SCHEDULER_ENABLED:-true}
      BILLING_SCHEDULER_INTERVAL_MS: ${BILLING_SCHEDULER_INTERVAL_MS:-60000}
    ports:
      - "8085:8085"
    volumes:
      - ./infrastructure/logs:/logs
    depends_on:
      consul-1:
        condition: service_healthy
      config-server:
        condition: service_healthy
      mysql:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    networks: [lxp-net]
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8085/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s


networks:
  lxp-net:
    name: lxp-net
    driver: bridge

volumes:
  grafana-data:
  loki-data:
  alloy-data:
