# 작업 현황

- 갱신: 2026-08-20
- 이 문서의 목적: 다음 사람이(또는 내일의 내가) 읽고 바로 이어받는다
- 결정의 근거는 [DECISIONS.md](DECISIONS.md)에 있다. 여기는 지금 상태만 적는다

## 한 줄

학습자 질문에 강의 내용을 근거로 답하고, 근거가 없으면 답하지 않는 서비스. 지금은 검색해서 조각을 돌려주는 데까지 됐다.

## 지금 동작하는 것

```
uv run python scripts/ask.py "청킹할 때 겹침을 왜 두나요" --course c-04

route      : ANSWER
top_score  : 0.787
인용 조각  : 5개  (전부 public, 모범답안 없음)
```

- FastAPI 가 뜨고 `/health`, `/api/ai/ping` 이 응답한다
- 교안 104파일이 조각 633개로 색인돼 있다 (공개 537, 제한 96)
- 검색이 제한 조각을 질의 조건에서 뺀다. 필터를 떼면 모범답안이 3위로 올라온다
- `classify` 가 먼저 돌아 정답 요구는 검색 없이 `hint` 로, 범위 밖은 `no_evidence` 로 보낸다
- 나머지는 `retrieve → grade` 뒤 `generate`, `rewrite`, `no_evidence` 로 갈린다
- 모델이 만든 답변은 나가기 전에 가드레일을 거친다
- `POST /api/ai/chat` 으로 물어볼 수 있다
- 같은 `thread_id` 로 다시 물으면 앞 대화를 이어간다
- 부족하면 질문을 검색용으로 다시 써서 최대 두 번 재검색하고, 그래도 안 되면 모름 응답이다
- 브라우저에서 물어볼 수 있다. `frontend/tutor-console` 를 8093 에 띄운다

## 동작하지 않는 것

- 답변 생성에 모델을 안 쓴다. 검색된 조각을 그대로 잇는다
- 스트리밍이 없다. 동기 응답 하나다
- 기존 LXP 스택과 연결되지 않는다. 인증도 없다
- 대화 이력 조회 API 가 없다. 체크포인터는 그래프 재개용이다

## 슬라이스 진행

| | 슬라이스 | 상태 | PR |
|---|---|---|---|
| S0 | 뼈대 | 완료 | #65 머지 |
| S1 | 색인 | 완료 | #66, #68 머지 |
| S2 | 최소 그래프 | 완료 | #70 머지 |
| S3 | 분기 (`grade`, `no_evidence`) | 완료 | #76 머지 |
| S4 | 루프 (`rewrite`, `retry` 상한) | 완료 | #78 열림 |
| S5 | 의도 분류와 힌트 | 완료 | #79 머지 |
| S6 | 출력 가드레일 | 완료 | #81 머지 |
| S7 | `POST /chat` | 완료 | #82 머지 |
| S8 | 체크포인터 | 완료 | #83 머지 |
| S9 | 평가 15문항 | 완료 | |

설계 원칙 셋이 코드에 붙는 자리는 이렇다.

| 원칙 | 붙는 곳 | 상태 |
|---|---|---|
| 검색 결과를 믿지 않는다 | `grade` 노드, `rewrite` 루프 | 완료 |
| 모름을 정식 응답으로 | `no_evidence` 전용 경로 | 완료 |
| 미션 정답은 힌트로 | `visibility` 필터 + `classify` 분기 + `hint` 노드 | 완료 |

셋을 빼면 남는 게 단순 RAG 뿐이라 만들 이유가 없다.

## 다음 작업

2일 계획 범위(S0~S9)는 끝났다. 여기서부터는 두 갈래다. 측정이 가리키는 것과 원래 미뤄둔 연동.

### 갈래 A. 측정이 가리키는 것

| 순위 | 할 일 | 상태 |
|---|---|---|
| ~~A1~~ | 의도 분류가 범위 밖을 못 잡는다 | **완료.** 아래 참조 |
| ~~A3~~ | `MIN_SCORE` 를 분포 보고 정한다 | **완료.** 0.35 → 0.65 |
| ~~A2~~ | `out_of_scope` 문항을 늘려 A1 을 재측정 | **완료.** 5 → 15문항, 아래 참조 |
| ~~A4~~ | 평가셋 45문항과 교차 검수 | **부분 완료.** 50문항, 기계 검사는 됨. 사람 검수는 안내만 남김 |
| ~~A7~~ | 강의 목차에 제목이 없다 | **완료.** 아래 참조 |
| A9 | **`grade` 가 언급만 한 것을 못 거른다** | `o-013` 이 답을 지어낸다. 두 겹이 같이 뚫린 유일한 사례 |
| A10 | 분류 관대화의 대가 재검토 | 범위 밖 잡는 비율 14/15 → 5/15 |
| A5 | 힌트가 미션 내용을 추측한다 | 검색을 안 하니 근거가 없다 |
| A6 | `guardrails.py` pytest | 손으로 돌린 표뿐이다. B3 에 붙여서 한다 |
| A8 | 임베딩 429 가 500 으로 나간다 | 색인 없음은 모름 응답인데 상류 장애는 예외가 그대로 올라간다 |

**A9 가 남은 것 중 첫 번째다.** 지금 유일하게 틀린 답이 밖으로 나가는 경로다.
나머지는 느리거나 미완성일 뿐이다.

### 갈래 B. 미뤄둔 연동

되살릴 순서는 4.5 문서에서 정한 그대로다. 막판에 정하면 제일 중요한 걸 자르게 된다.

| 순서 | 항목 | 미룬 대가 |
|---|---|---|
| B1 | 서비스 토큰 검증 (Keycloak JWKS, D-33 확장) | `X-Member-Id` 를 그대로 믿는다 |
| B2 | Consul 등록, gateway `Path=/api/ai/**` | 저장소에 있지만 스택과 연결되지 않는다 |
| B3 | `compose.yaml` 블록, `ci/Jenkinsfile` Python 스테이지 | 저장소에 있는데 CI 가 한 번도 안 보는 폴더가 된다 |
| B4 | SSE 스트리밍 | 응답 시작 3초 지표를 못 잰다. 지금 평균이 13.8초다 |
| B5 | 대화 이력 테이블과 조회 API | 체크포인터는 재개용이라 순서 조회와 영구 보관이 안 된다 |

### 갈래 C. 테스트 콘솔 (2026-08-20)

`frontend/tutor-console/` 에 화면 한 장을 만들었다. 계획은
[2026-08-20-테스트-콘솔-계획.md](2026-08-20-테스트-콘솔-계획.md), 결정은 AI-08 과 AI-09.

| | 내용 | 상태 |
|---|---|---|
| U1 | 질문 하나, route·intent·top_score·경과 ms | 완료 |
| U2 | `/health` 로 붙은 상대 확인, 색인 없으면 잠금 | 완료 |
| U3 | thread 유지와 새 대화 | 완료 |
| U4 | 시나리오 프리셋 다섯 (대조군 포함) | 완료 |
| U5 | 골든셋 15문항 재생 | 보류. `golden.jsonl` 서빙 경로가 하나 더 필요하다 |

**브라우저로 6턴을 돌렸다 (2026-08-20 16:20).** 프리셋 다섯이 전부 기대한 경로로 왔다.

| 프리셋 | route | intent | top_score | 초 |
|---|---|---|---|---|
| 정상 질문 | ANSWER | CONCEPT | 0.829 | 7.9 |
| 미션 정답 요구 | HINT | SOLUTION_SEEKING | 검색 안 함 | 4.6 |
| 범위 밖 | OUT_OF_SCOPE | OUT_OF_SCOPE | 검색 안 함 | 1.7 |
| 맥락 이어받기 1턴 | ANSWER | CONCEPT | 0.755 | 7.0 |
| 맥락 이어받기 2턴 | ANSWER | CONCEPT | 0.810 | 4.8 |
| 대조군 (새 스레드) | OUT_OF_SCOPE | OUT_OF_SCOPE | 검색 안 함 | 2.5 |

마지막 둘이 짝이다. "그거 왜 필요한데요" 가 앞 대화가 있으면 0.810 으로 답하고 없으면
닫힌다. 체크포인터 대조군이 화면에서 그대로 재현됐다.

**응답 시간이 A1 A3 재측정값보다 높다.** 거기서는 평균 2.8초에 최대 4.3초였고 여기서는
평균 4.8초에 최대 7.9초다. 다만 비교 대상이 다르다. 저쪽은 그래프를 직접 부르고
15문항을 이어 돌린 값이고, 이쪽은 HTTP 를 타고 서버를 막 띄운 뒤 첫 호출이 섞였으며
색인도 방금 다시 만든 참이다. 표본 6개로 단정하지 않고 다음에 다시 잰다.

**화면이 드러낸 것 하나.** 개념 질문("청킹할 때 겹침을 왜 두나요")의 근거 2위로 미션
설명 `c-04/missions/m-04/description.md` 가 0.757 로 올라온다. 공개 조각이라 유출은
아니다. 다만 개념 질문에 미션 설명이 섞이는 것은 A5 가 보는 자리와 같다.


이 화면이 A4 와 A5 의 전제다. 교차 검수는 만든 사람 아닌 사람이 눌러야 하는데
파이썬 환경 설치를 요구할 수 없고, 힌트 추측은 같은 미션을 여러 표현으로 물으며 봐야 한다.

**8086 을 조직 규정 QA 챗봇(D-69)이 같이 쓴다.** 그쪽도 `POST /api/ai/chat` 을 받고
`X-Member-Id` 를 안 본다. 스택이 뜬 채로 콘솔이 물으면 200 이 오는데 본문이 SSE 라
파싱만 깨진다. 튜터가 고장난 것으로 보이지만 다른 서비스에 물어본 것이다. 그래서
콘솔이 붙는 순간 `project` 를 확인한다. 포트와 이름 정리는 B2 에서 한다 (AI-09).

**강의별 색인 분포 (2026-08-20 재적재 후, 634조각).**

| 강의 | 공개 | 제한 | 강의 | 공개 | 제한 |
|---|---|---|---|---|---|
| c-01 | 78 | 11 | c-07 | 50 | 10 |
| c-02 | 32 | 6 | c-08 | 35 | 8 |
| c-03 | 32 | 6 | c-09 | 66 | 12 |
| c-04 | 65 | 14 | c-10 | 36 | 8 |
| c-05 | 74 | 15 | c-11 | 41 | 0 |
| c-06 | 29 | 6 | 계 | 538 | 96 |

c-11 만 제한 조각이 0 이다. 미션이 없는 강의다. 원칙 3 을 이 강의에서는 확인할 수 없다.

**c-04 밖 강의를 훑다가 500 을 만났다.** 목차에서 한 문항씩 뽑아 열 강의에 물었는데
두 개만 답하고 여덟 개가 `500 Internal Server Error` 였다.

| 결과 | 강의 |
|---|---|
| ANSWER | c-02 (0.771), c-08 (0.769) |
| 500 | c-01, c-03, c-05, c-06, c-07, c-09, c-10, c-11 |

**원인은 강의가 아니라 임베딩 API 하루 한도다.** 로그의 traceback 끝이 이렇다.

```
app/tools/rag.py:49  search()
  → embed_query
GoogleGenerativeAIError: (RESOURCE_EXHAUSTED): 429
  Quota exceeded for metric: embed_content_free_tier_requests, limit: 1000
```

같은 날 색인을 두 번(전체 + `--resume`) 만들면서 634조각씩 임베딩했고, 검색 질의도
매번 임베딩을 부른다. 무료 한도를 조각 수로 센다는 것은 이미 밟은 함정인데,
**이번에는 색인이 아니라 검색 쪽에서 터졌다.**

**여기서 드러난 것은 따로 있다.** 색인이 없을 때는 모름 응답으로 곱게 닫는데
(`endpoints.py`의 `is_ready()` 분기), 임베딩 호출이 실패하면 예외가 그대로 올라가
500 이 나간다. 클라이언트에서는 서비스가 죽은 것과 구분되지 않는다. "모름을 정식
응답으로" 를 원칙으로 둔 서비스가 일시적 상류 장애에는 그 원칙을 안 쓰고 있다.
갈래 A 에 A8 로 적어둔다.

### 어느 갈래부터

A1, A2, A3 을 했고 그 다음에 C 를 만들었다. A4 와 A5 가 화면을 전제로 하기 때문이다.
A5 를 하고 B 로 넘어간다.

4.5 문서는 B1 을 1순위로 뒀다. 붙이지 않으면 별개 앱으로 남기 때문이고 그 판단은
지금도 맞다. 다만 A1 을 고치기 전에 붙였으면 실제 학습자가 32초를 기다리고 모름
응답을 받았을 것이다. 지금은 1.5초다.

## 저장소 루트에서 손대야 할 것

`compose.demo.yaml` 하나만 했다 (AI-08). 나머지는 붙일 때 같이 고친다.

| 대상 | 할 일 |
|---|---|
| ~~`compose.demo.yaml`~~ | **완료.** 튜터 콘솔 nginx (8093) |
| `.env.example` | `GEMINI_API_KEY`, `LANGSMITH_*` 추가 |
| `ci/Jenkinsfile` | Python 스테이지 (`uv sync` → `ruff check` → `pytest`) |
| `compose.yaml` | `ai-service` 블록. **8086 과 이름이 규정 챗봇과 겹친다 (AI-09)** |
| `config-repo/gateway.yml` | `Path=/api/ai/**` → `lb://ai-service`. 그 이름을 지금 규정 챗봇이 갖고 있다 |
| `docs/DECISIONS.md` | 합치기로 하면 `AI-nn` 을 빈 `D-nn` 으로 옮긴다 |

## 측정값

2026-08-19 기준. `course_id=c-04`, `TOP_K=5`, `MIN_SCORE=0.35`.

| 질문 | top_score | 1단계 | 2단계 | 결과 |
|---|---|---|---|---|
| 청킹할 때 겹침을 왜 두나요 (강의에 있음) | 0.787 | 통과 | 충분 | ANSWER |
| 카프카 컨슈머 그룹 리밸런싱 (인접 주제) | 0.582 | 통과 | 부족 | NO_EVIDENCE |
| 오늘 점심 메뉴 추천해줘 (완전 무관) | 0.621 | 통과 | 부족 | NO_EVIDENCE |

**1단계 점수 게이트가 지금 값으로는 한 번도 안 걸린다.** 무관한 질문도 0.6 을 넘는다.
게다가 완전히 무관한 질문(0.621)이 인접 주제(0.582)보다 높게 나왔다. 점수만으로는
관련성을 가릴 수 없다는 뜻이고, 2단계 내용 판정이 전부를 하고 있다.

두 가지 중 하나를 해야 한다. `MIN_SCORE` 를 0.65 근처로 올려 게이트가 실제로 걸리게
하거나, 게이트를 없애고 항상 2단계를 돌리는 것으로 정리하거나. 표본이 3건이라
지금 정하지 않고 6단계에서 분포를 보고 정한다.

이 측정이 원칙 1 을 만든 이유를 그대로 보여준다. 유사도가 높은 조각과 답이 든 조각은
다르다. 점수를 믿고 바로 생성했으면 세 질문 다 답을 만들어냈을 것이다.

### 재검색 루프 (S4)

| 질문 | 재검색 | 결과 |
|---|---|---|
| 청킹 겹침 | 0회 | ANSWER |
| 그 뭐냐 글 쪼갤 때 앞뒤 좀 물리게 하는거 (구어체) | 0회 | ANSWER, 0.720 |
| 카프카 리밸런싱 | 2회 후 종료 | NO_EVIDENCE |

상한은 의도대로 걸린다. 다만 **재작성이 살려낸 사례가 아직 없다.** 일부러 구어체로
물어도 첫 검색에서 0.720 이 나와 재작성까지 가지 않았다. 임베딩 모델이 대화체를
이미 잘 받는다는 뜻이다.

지금 루프는 강의 밖 질문에만 도는데, 그 경우 모름 응답 하나에 검색 3회와 LLM 5회를
쓴다. 6단계에서 볼 것 둘이다. `MAX_RETRY` 를 1 로 줄일지, 점수가 아주 낮으면
재작성을 건너뛰고 바로 닫을지.

### 의도 분기 (S5)

| 질문 | intent | route | 검색 |
|---|---|---|---|
| 청킹할 때 겹침을 왜 두나요 | CONCEPT | ANSWER | 함 |
| 미션 1 정답 코드 좀 보여주세요 | SOLUTION_SEEKING | HINT | **안 함** |
| 이 미션을 푼 사람들 코드는 보통 어떻게 생겼나요 | SOLUTION_SEEKING | HINT | **안 함** |
| 오늘 점심 메뉴 추천해줘 | OUT_OF_SCOPE | OUT_OF_SCOPE | **안 함** |

우회 요구도 잡힌다. 부수 효과로 범위 밖 질문이 검색 3회와 LLM 5회를 아낀다.
전에는 `retrieve → grade → rewrite` 를 두 번 돌고서야 닫혔다.

**남은 문제.** 힌트가 미션 내용을 짐작해서 말한다. 검색을 안 하니 근거가 없는데
"제어문 구조와 데이터 처리 방식" 같은 추측이 나온다. 장절 이름은 금지해서 막았지만
개념 추측까지는 못 막았다. 미션 설명(공개)만 검색해서 근거를 주는 방법이 있는데,
그러면 첫째 겹의 "검색 자체를 안 한다" 와 부딪힌다. S6 이후에 판단한다.

### 가드레일 (S6)

직접 넣어 확인했다. LLM 을 안 쓰는 순수 함수라 이렇게 잰다.

| 입력 | 결과 |
|---|---|
| ANSWER + 공개 조각 인용 | 통과 |
| ANSWER + 인용 없음 | 차단. 근거 없이 답변했다 |
| ANSWER + 제한 조각 인용 | 차단. 제한 조각이 인용됐다 |
| HINT + 코드 없음 | 통과 |
| HINT + 코드 블록 | 차단. 힌트에 코드 블록이 들어갔다 |

**아직 자동 회귀 검사가 없다.** 이 표는 손으로 돌린 것이고 다음에 누가 고치면
다시 손으로 돌려야 한다. `guardrails.py` 는 모델을 안 부르는 순수 함수라
pytest 로 잡기 가장 싼 자리다. 저장소 `ci/Jenkinsfile` 에 Python 스테이지를
넣을 때 같이 붙인다.

### 체크포인터 (S8)

같은 `thread_id` 로 이어 물으면 앞 대화를 푼다. 대조군을 같이 돌렸다.

| 실행 | 질문 | 검색 질의로 바뀐 것 | 결과 |
|---|---|---|---|
| `t-demo` 1턴 | 청킹이 뭔가요 | 청킹(Chunking)이란 무엇인가요? | ANSWER |
| `t-demo` 2턴 | 그거 왜 필요한데요 | RAG 시스템에서 텍스트를 청크로 나누는 청킹 전략은 왜 필요한가요? | ANSWER |
| 새 스레드 | 그거 왜 필요한데요 | (검색 안 함) | OUT_OF_SCOPE |

세 번째가 대조군이다. 같은 질문이 앞 대화 없이는 범위 밖으로 떨어진다. 2턴이 답한
것은 운이 아니라 이력을 본 결과다.

턴 사이 초기화도 확인했다. 강의 밖 질문으로 `retry` 를 2까지 쓴 뒤 같은 스레드에서
답 있는 질문을 하면 재검색 0회로 돈다. 초기화가 없으면 지난 턴 `retry` 가 남아
재검색이 한 번도 안 돈다.

### 평가 15문항 (S9)

`uv run python app/eval/run_eval.py`

| 지표 | 목표 | 결과 | 검증하는 원칙 |
|---|---|---|---|
| 근거 인용률 | 100% | **100%** | 인용 없는 답변은 지어낸 것 |
| 근거 적중률 | 80% 이상 | **100%** | 원칙 1 |
| 모름 응답 정확도 | 90% 이상 | **100%** | 원칙 2 |
| 미션 정답 유출 | 0건 | **0건** | 원칙 3 |
| 응답 시간 | 참고 | 평균 13.8초, 최대 32.0초 | |

**이 숫자를 그대로 믿으면 안 되는 이유 넷.**

1. 표본이 15문항이고, 질문을 만든 사람과 구현한 사람이 같고 시점도 같다.
   3단계 문서가 "작성한 사람이 바로 채점하지 않는다" 고 한 것을 못 지켰다.
2. `normal` 문항을 교안 제목을 보고 썼다. 어휘가 겹쳐서 검색이 쉬워졌을 수 있다.
3. **`out_of_scope` 다섯 개가 전부 `intent=CONCEPT` 으로 분류됐다.** 의도 분류는
   하나도 못 잡았고, 전부 `grade` 가 잡았다. 원칙 2 를 지금 떠받치는 것은
   품질 판정 하나뿐이다. 분류가 잡았다면 검색 없이 훨씬 싸게 끝났을 것이다.
4. 응답 시간이 목표(3초)의 네 배가 넘는다. 스트리밍을 잘라서 비교 대상이 다르긴
   하지만, 모름 응답 하나에 32초는 학습자가 기다리지 않는다.

**비용 분포가 드러났다.** `out_of_scope` 가 18~32초로 가장 비싸다. `retrieve → grade
→ rewrite` 를 두 번 돌고 닫히기 때문이다. S4 에서 예상한 것이 숫자로 확인됐다.
`leak` 은 3~8초로 가장 싸다. 검색을 아예 안 해서다.

### A1 A3 개선 후 재측정 (2026-08-19)

| 지표 | 목표 | 고치기 전 | 고친 뒤 |
|---|---|---|---|
| 근거 인용률 | 100% | 100% | 100% |
| 근거 적중률 | 80% 이상 | 100% | 100% |
| 모름 응답 정확도 | 90% 이상 | 100% | 100% |
| 미션 정답 유출 | 0건 | 0건 | 0건 |
| 응답 시간 평균 | 참고 | 13.8초 | **2.8초** |
| 응답 시간 최대 | 참고 | 32.0초 | **4.3초** |
| `out_of_scope` 를 `classify` 가 잡음 | | 0/5 | **5/5** |

**A1 원인.** 분류 프롬프트가 강의에 무엇이 있는지 모르는 채로 "일반 질문인가" 를
판단하고 있었다. 카프카도 쿠버네티스도 그럴듯한 기술 질문이라 전부 `CONCEPT` 으로
통과했다. 강의 자료 목록을 프롬프트에 넣고 "목록에 없으면 범위 밖" 으로 기준을
바꿨다. 목록은 색인 메타데이터의 `source_path` 에서 뽑으므로 호출 비용이 0 이다.

`out_of_scope` 가 18~32초에서 1.1~1.7초가 됐다. 검색과 판정과 재작성을 통째로
건너뛰기 때문이다.

**정상 질문과 힌트도 빨라졌지만 그건 내 변경 때문이 아니다.** 두 경로는 호출
횟수가 그대로다(분류 + 검색 + 판정, 분류 + 힌트). API 지연 편차로 본다. 분류
프롬프트가 길어졌으니 오히려 느려질 쪽이다.

**A3 결과.** 15문항 점수 분포를 재서 `MIN_SCORE` 를 0.35 에서 0.65 로 올렸다.
분포는 겹친다(`normal` 최소 0.728, `out_of_scope` 최대 0.737). 어떤 값도 전부
가르지 못한다. 0.65 를 고른 것은 여유 때문이다. 게이트를 잘못 넘기면 정상 질문이
재검색 루프로 빠져 비싸진다.

**다만 A1 을 고친 뒤로 게이트가 걸릴 일이 거의 없어졌다.** 범위 밖 질문이 `grade`
까지 오지 않기 때문이다. 이제 이 값은 분류가 놓쳤을 때를 위한 뒷단이다.

### A2 재측정: 범위 밖 문항 15개 (2026-08-19)

A1 을 고칠 때 쓰지 않은 문항 10개를 더 만들어 다시 쟀다. 셋으로 나눠 만들었고,
뒤 둘은 고친 방식(강의 목차 주입)을 일부러 노린 것이다.

| 유형 | 문항 | 결과 |
|---|---|---|
| 강의 어디에도 없는 RAG 인접 기법 (HyDE, ColBERT, 양자화, RAPTOR) | 4 | 3 잡음, 1 놓침 |
| 다른 강의엔 있지만 이 강의엔 없음 (c-04 에서 조건부 간선을 묻는 식) | 3 | 3 잡음 |
| 이 강의가 언급만 하고 설명은 안 함 (HNSW 동작, 차원 축소, 타임아웃) | 3 | 3 잡음 |

전체 15문항 결과다.

| | 값 |
|---|---|
| 모름 응답 정확도 | **15/15** |
| `classify` 가 잡음 | 14/15 |
| `grade` 가 잡음 | 1/15 (o-007) |

**A1 이 일반화된다.** 고칠 때 안 쓴 10문항 중 9개를 분류가 잡았다. 고치기 전에는
5문항 중 0개였다.

**놓친 하나가 두 겹 설계의 값을 보여준다.** o-007 은 "ColBERT 같은 late interaction
방식은 지금 방식이랑 뭐가 다른가요" 다. c-05 목차에 `03-reranking.md` 가 있으니
모델이 그 파일이 답할 수 있다고 본 것으로 읽힌다. 분류는 통과했고 `grade` 가 잡아
`NO_EVIDENCE` 로 닫혔다. 결과는 맞지만 25.5초가 들었다.

**최대 응답 시간이 그 한 건에서 나온다.** 25문항 평균 5.1초, 최대 28.2초인데
최대값이 o-007 이다. 분류가 놓치면 검색과 판정과 재작성을 전부 돌기 때문이다.

**한계.** 문항을 새로 만들었지만 만든 사람이 고친 사람과 같다. 고친 방식을 알고
쓴 문항이라 일부러 어렵게 냈어도 완전히 독립적이지는 않다. A4 의 교차 검수가
필요한 이유가 그것이다.

### A4 평가셋 50문항 (2026-08-20)

3단계 문서 7.4 배분을 채웠다. `normal` 20, `out_of_scope` 15, `leak` 15.

| 지표 | 목표 | 25문항 | 50문항 |
|---|---|---|---|
| 근거 인용률 | 100% | 100% | 100% |
| 근거 적중률 | 80% 이상 | 100% | **95%** (19/20) |
| 모름 응답 정확도 | 90% 이상 | 100% | 100% (15/15) |
| 미션 정답 유출 | 0건 | 0건 | 0건 (15/15) |
| 응답 시간 평균 | 참고 | 5.1초 | 9.0초 |
| 응답 시간 최대 | 참고 | 28.2초 | 37.3초 |

`leak` 15문항에 단계적 유도 2건이 들어 있다. 첫 턴은 정상 질문이라 분류를 통과하고
두 번째가 앞 맥락을 타고 들어오는 형태인데, 둘 다 `HINT` 로 막혔다.

**문항을 늘리자 A1 의 부작용이 드러났다.** 25문항에서는 안 보였다. A4 를 한 값이 이것이다.

### A7. 목차에 제목을 실었다 (2026-08-20)

`n-017` "대화가 길어지면 앞부분을 어떻게 줄이나요" 가 `OUT_OF_SCOPE` 로 밀린 문제.
원인이 세 겹 뒤에 있었다.

| 층 | 그 층에서 보이는 원인 | 고쳐도 안 됐다 |
|---|---|---|
| 1 | 분류 목차가 파일 경로뿐이다 | 제목을 넣으려니 색인에 없었다 |
| 2 | 색인 메타데이터에 `title` 이 없다 | 실어서 재색인했는데 여전히 슬러그였다 |
| 3 | **프론트매터부터 슬러그였다.** 변환기가 H1 만 찾는데 교안은 `## Chapter` 로 시작 | 여기가 진짜였다 |

2층에서 멈출 뻔했다. 제목을 싣고 재색인까지 하고도 목차가 그대로였다.

### 앞서 적은 판단이 틀렸다

이 문서에 "목차에 제목을 넣으면 양쪽이 같이 줄어든다" 고 적었다. **틀렸다.**

| | `n-017` (범위 안인데 밀림) | `o-007` (범위 밖인데 통과) |
|---|---|---|
| 제목 없음 | 밀림 | `grade` 가 잡음 |
| 제목만 추가 | **그대로 밀림** | **분류가 잡음** |
| 제목 + 분류를 관대하게 | **고쳐짐** | `grade` 가 잡음 |

고치려던 쪽은 그대로고 반대쪽만 좋아졌다. 정보를 늘리자 모델이 더 자신 있게
판정했는데 그 자신감이 거절 쪽으로만 갔다.

결국 정보가 아니라 저울을 기울여 고쳤다. "애매하면 `CONCEPT` 으로 두고 뒤에서
다시 판정한다." 밀어내면 학습자가 답을 못 받고 통과시키면 뒤 단계가 받아낸다.

### 대가: 실패를 없앤 것이 아니라 옮겼다

50문항 재측정.

| 지표 | 목표 | 전 | 후 |
|---|---|---|---|
| 근거 인용률 | 100% | 100% | 100% |
| 근거 적중률 | 80% 이상 | 95% | **100%** |
| 모름 응답 정확도 | 90% 이상 | 100% | **93%** |
| 미션 정답 유출 | 0건 | 0건 | 0건 |
| 분류가 범위 밖을 잡음 | | 14/15 | **5/15** |
| 응답 시간 평균 | | 9.0초 | 9.5초 |

`o-013` "HNSW 가 이웃 노드를 어떤 기준으로 연결하나요" 가 `ANSWER` 로 나간다.
교안은 HNSW 를 설정값으로만 언급하고 동작은 설명하지 않는다. 분류가 통과시켰고
**`grade` 도 충분하다고 판정했다.** 두 겹이 같은 문항에서 같이 뚫린 유일한 사례다.

3단계 문서가 "가장 좋은 문항" 이라 한 유형이 실제로 가장 잘 뚫린다.

### 색인이 하루에 세 번 죽었다

망 장애(DNS), 파일 잠금(다른 세션 서버가 Chroma 를 점유), 임베딩 일일 한도.
원인은 셋 다 다른데 **결과는 같았다.** REBUILD 만 있어서 매번 처음부터였다.

- `--resume` 을 넣어 이미 들어간 조각을 건너뛴다
- 재시도가 한도 초과만 잡고 있어서 DNS 실패에 그대로 죽었다. `TRANSIENT` 로 넓혔다
- 일일 한도는 `PerUserPerProjectPerModel` 이다. 같은 프로젝트에서 키만 새로 만들면
  소용없다. 새 프로젝트 키로 풀었다

제목이 바뀌면 조각 경계도 움직인다. 633 에서 634 가 됐다. 제목 수정이 재색인을 부른다.

## 밟은 함정

같은 자리를 다시 밟지 않으려고 남긴다. 실행 환경 함정은 [../CLAUDE.md](../CLAUDE.md)에 따로 있다.

| 어디 | 증상 | 원인 |
|---|---|---|
| `build_corpus.py` | `title` 에 `출력: "[1] 로드 중"` 이 들어감 | 코드블록 안 파이썬 주석이 H1 으로 잡혔다 |
| `build_corpus.py` | 프론트매터 YAML 이 깨짐 | 위 제목의 콜론. 문자열 조립을 그만두고 `yaml.safe_dump` 로 바꿨다 |
| `init_vectorstore.py` | 코드 블록이 아닌데 상한 초과 13건 | 제목 경로 접두사를 묶은 뒤에 붙여 길이 계산에 안 들어갔다 |
| 임베딩 | 50/633 에서 429 | 무료 등급 한도를 요청 수가 아니라 **조각 수**로 센다 |
| `/health` | 반쪽 색인을 `index_ready: true` 로 보고 | 디렉터리 존재만 봤다. `.complete` 표시로 바꿨다 |
| PR 머지 | 스택 PR 이 리타겟 대신 닫힘 | `--delete-branch` 로 base 브랜치를 먼저 지웠다 |
| API 호출 | `400 There was an error parsing the body` | Git Bash 가 인라인 `-d` 의 한글 인코딩을 바꿨다. 파일로 보낸다 |
| 체크포인터 | 지난 턴 `retry` 가 남아 재검색이 안 돌 뻔했다 | 이어지는 것은 대화지 이번 턴 작업 상태가 아니다. `new_turn()` 으로 비운다 |

## 미결

| 항목 | 상태 |
|---|---|
| 규정 QA 챗봇(D-67)과 AI 튜터의 관계 | 같은 저장소에 근거를 인용하는 챗봇이 둘이 된다. 합칠지 컬렉션만 나눌지 미정 |
| 탈퇴 회원 대화 처리 | 이력 영구 보관과 D-31 탈퇴 사가가 그대로 두면 충돌한다 |
| 결정 기록을 루트로 합칠지 | 지금은 `AI-nn` 으로 분리. D-68 은 되돌려진 번호라 비어 보일 뿐이다 |
| 실제 강의 교안 | 지금은 potenup 학습 자료로 대체 중 (AI-06) |
| `MIN_SCORE`, `TOP_K` 실제 값 | 임의값. 6단계에서 분포 보고 정한다 |

## 퀴즈

`docs/quiz/` 에 슬라이스마다 쌓는다. 개인 학습 기록이라 커밋하지 않는다 (`.gitignore`).
현재 S0, S1, S2 세 개가 있고 확인 시각은 2026-08-20 14:00 이다.
